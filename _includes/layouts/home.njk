---
layout: base
permalink: "/"
---

<div class="home-intro">
  {{ content | safe }}
</div>
<h2 class='projects-heading'>Case Studies & Concepts</h2>

<p>Deep dives into my projects, both for clients and app concepts, read on to find out about how I approach UX Design in a project and all about my process.<p>
{% set caseStudies = collections.caseStudies %}
<ul class='case-studies'>
{% for project in caseStudies %}
      <li>
        <a href="{{ project.url }}">
                 {% if project.data.icon %}
          <img
            loading="lazy"
            src="/media/{{ project.data.icon }}"
            alt=""
            class='case-studies-icon'
          >
          {% endif %}
        <div class='case-studies-text'>
        <h3>{{ project.data.title | safe }}  →</h3>
        <p>{{ project.data.description | safe }}</p>
        </div>
        </a>
      </li>
  {% endfor %}
  {% set conceptProjects = collections.concept %}
  {% for project in conceptProjects %}
    {% if project.inputPath.includes('/projects/') %}
      <li>
    <a href="{{ project.url }}">
       {% if project.data.icon %}
          <img
            loading="lazy"
            src="/media/{{ project.data.icon }}"
            alt=""
            class='concepts-icon'
          >
          {% endif %}
<div class='concepts-text'>
        <h3>{{ project.data.title | safe }}  →</h3>
    <p>{{ project.data.description | safe }}</p>
    </div>
    </a>
      </li>
    {% endif %}
  {% endfor %}
</ul>
<h2 class='projects-heading'>Explorations & Other designs</h1>
<p>A space for me to explore different designs.</p>
{% set explorationsProjects = collections.explorations %}
<ul class='grid'>
  {% for project in explorationsProjects %}
    {% if project.inputPath.includes('/projects/') %}
      <li class='explorations'>
        <a href="{{ project.url }}">
          {% if project.data.image %}
          <img
            loading="lazy"
            src="/media/{{ project.data.image }}"
            alt=""
            class='explorations-img'
          >
          {% endif %}
          <h3>{{ project.data.title | safe }}  →</h3>
        </a>
      </li>
    {% endif %}
  {% endfor %}
</ul>

<h2 class='projects-heading'>Writing</h2>
<p>A collection of my writing.</p>
<ol>
{% for post in collections.posts %}
  <li>
    <a href="{{ post.url }}">
      <h3>{{ post.data.title }}</h3> </a>
      <p>{{ post.data.description }}</p>
      <p>Written on {{ post.data.date | date("MMMM DD, YYYY") }}</p>
  </li>
{% endfor %}
</ol>
<a href='/posts'>Read All →</a>


<h2>Cinematography Stills</h2>
<p>I spent a few years behind the camera, capturing cool shots for some cool projects. Check out these stills from some of my favorites.</p>
    <div class="app">
      <div class="blaze-slider my-slider">
        <div class="blaze-container">
          <div class="blaze-track-container">
            <div class="blaze-track">
{% for item in collections.cinematography %}
      <img src="{{ item.src }}" class="image-item" alt="{{ item.alt }}" />
  {% endfor %}
            </div>
          </div>
        </div>
                <div class="controls">
          <button class="blaze-prev">←</button>
          <button class="blaze-next">→</button>
        </div>
      </div>
    </div>
