---
layout: base
permalink: "/projects/{{ page.fileSlug }}/index.html"
---

<article class="project">
  <header class="project-header">
    <h1 class="project-title">{{ title | safe }}</h1>
    {% if dateStart or dateEnd %}
      <p class="project-details">{{ dateStart | yearRange(dateEnd) | safe }}</p>
    {% endif %}
    <div class='spec-container'>
    <div class='spec'><h3>Role &mdash;</h3><p class='home-block-role'>{{ entry.data.role | safe}}</p></div>
    <div class='spec'><h3>Completion Date &mdash;</h3><p class='home-block-completion-date'>{{ entry.data.completionDate | safe}}</p></div>
    <div class='spec'><h3>Read Time &mdash;</h3><p class='home-block-readtime'>{{ entry.data.readTime | safe}}</p></div>
    <div class='spec'><h3>Platform &mdash;</h3><p class='home-block-platform'>{{ entry.data.platform | safe}}</p></div>
    </div>
    {% if content %}
      <div class="project-text">
        {{ content | safe }}
      </div>
    {% endif %}
  </header>
</article>

{% set next = collections.projects | nextInCollection(page.fileSlug) %}
{% set previous = collections.projects | prevInCollection(page.fileSlug) %}

{% if next or previous%}
  <div class="pagination">
    <div>
    {% if previous %}
      <a class="pagination-item pagination-item-prev" href="{{ previous.url }}">
        <svg width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10.25 6.75L4.75 12L10.25 17.25"></path><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.25 12H5"></path></svg>
      <p>Previous project</p>
      </a>
    {% endif %}
    </div>
    <div>
    {% if next %}
      <a class="pagination-item pagination-item-next" href="{{ next.url }}">
        <p>Next project</p>
        <svg width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.75 6.75L19.25 12L13.75 17.25"></path><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 12H4.75"></path></svg>
      </a>
    {% endif %}
    </div>
  </div>
{% endif %}